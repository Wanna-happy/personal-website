# 当前相册管理入口（2026-09-20 更新）

相册已并入需要管理员密码登录的 `admin.html#photos`。旧 `?album=edit` 地址会跳转到管理后台，不再免登录编辑。

首次运行项目根目录「设置管理密码.cmd」，然后打开「打开网站管理.cmd」。后台支持上传照片、编辑说明、拖动夹子或键盘排序、删除/撤销；点击保存后会写入本地网站文件，刷新预览可见。首次进入会兼容读取同一浏览器旧 IndexedDB 中的照片，不会清空旧记录。

保存到本地不等于部署到 GitHub Pages。详细保存范围与发布说明见 [ADMIN.md](ADMIN.md)。

以下保留旧版记录，仅用于追溯：

---

# 生活相册

最后一张空相片始终是添加入口，添加照片后会继续补上空相片。已有照片可放大、上一张/下一张浏览、删除和撤销删除。手机支持点按与大图左右滑动，键盘支持方向键与 Escape。

## 当前保存范围

网页用 IndexedDB 保存新增照片和删除记录，保留原图并为新增照片生成小预览图。同一浏览器、同一网站地址下刷新仍在；不同浏览器、设备、端口或清除网站数据后不共享这些修改。界面明确标注保存范围。原始文件不会因点击删除从磁盘或仓库中移除。

“导出相册备份”导出当前照片清单，新增照片包含完整图片数据。公开网站尚未接入管理员身份和服务端存储，添加/删除不会自动修改 GitHub 仓库，也不会改变其他访客看到的内容。后续公开同步需接入具有管理员权限校验的存储与发布流程。

## 维护发布内容

默认照片清单在 `photo-data.js`。后续发布时，将照片放入 `assets/life/` 并增加 `{id, src, alt, caption}` 条目；可选 `thumbnail`。ID 保持稳定，不要用已有 ID 代表另一张照片。删除已发布内容时移除对应清单条目，再按原网站流程发布。布局自动换行，空相片会自动生成，不需要手动复制 HTML。

收到导出的 `ruan-life-album.json` 后，维护者可把 data URL 图片解码成静态文件，更新清单并检查后发布。没有生成云端上传成功的假提示。

## 紧凑相片与管理入口

访客页面展示缩小后的纸质相片和“尽情期待”空相片；没有上传、删除、编辑或排序控件。宽照片完整显示，相框高度贴合内容，不跨两列放大，也不强行留出竖向大片空白。桌面卡片最大宽 150 px，挂线按实际行高排布。

本机管理入口：http://127.0.0.1:8765/?album=edit#about 。仅在 loopback 地址带 `album=edit` 时启用编辑；公开域名即使带此参数，也不开启管理。这是本地编辑模式，不是账号登录/服务端身份验证。

管理页直接拖动相片上方夹子排序，支持鼠标和触摸；键盘聚焦夹子后用左右方向键调整。点击相片下文字可编辑、保存或取消，支持 Escape 与 Ctrl/Command + Enter。删除可撤销，末尾保留上传入口。

排序、文字修改与添加/删除共用本机 IndexedDB，刷新会保留；导出备份包含当前顺序。已有相册兼容读取，不会清空。本地展示页读取同一浏览器中的保存结果；跨设备/公开发布仍需另行接入存储与发布流程。
