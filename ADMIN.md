# 网站管理

## 在线使用

1. 打开 https://wanna-happy.github.io/personal-website/admin.html ，也可点击网站右上角「进入后台」。
2. 输入已设置的管理员密码。
3. 编辑后点击「保存并发布」。后台提交内容与附件到仓库，页面显示发布进度；发布完成后刷新公开网站查看。
4. 完成后点击「退出登录」。会话一小时后过期，退出会立即撤销。

不需要启动本机 Python。云端服务闲置后首次打开可能需要等待启动。密码由服务端验证；浏览器只保存短期会话标识。

## 可维护内容

- 简介、教育、课程、联系信息与欢迎语。
- 相册：上传照片、编辑说明、排序、删除与撤销删除。
- 项目：封面、简介、问题/过程/结果、链接、讲解文稿、PDF 与多页展示图片；支持添加、修改、排序和移除。PPTX 请先导出为 PDF 或图片。
- 经历：组织、职位、时间、内容、成果、标签和讲解文稿；支持添加、修改、排序和移除。

新增讲解文稿以文字显示，需另外配置对应录音，不会套用旧录音。

## 保存与恢复

保存更新 content.json、content-data.js 和引用的 assets/uploads/ 文件。Git 提交保留内容历史；删除条目不会物理删除原始附件。上传后尚未保存的附件不会展示给访客。

多个编辑窗口发生版本冲突时，后台拒绝覆盖。请先导出当前草稿备份，再载入最新内容合并。导出文件为内容清单，图片与附件仍需保留。

在线权限由服务器管理，不要把令牌或云密钥写入前端。环境和凭据需要在到期前续期，见 [online-admin/README.md](online-admin/README.md)。

## 本地方式（可选）

双击「打开网站管理.cmd」启动本地预览和 Python 管理服务。首次使用可通过「设置管理密码.cmd」设置本机密码。本地服务只监听回环地址，本地保存需要另行提交推送，不会自动同步云端密码或发布。
