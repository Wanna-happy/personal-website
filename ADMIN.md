# 网站管理

## 使用

1. 首次在项目根目录双击「设置管理密码.cmd」，设置至少 8 位密码（输入不会回显）。密码和确认均由本人在本机输入，不经过聊天。
2. 双击「打开网站管理.cmd」，或在网站右上角模式切换旁点击「进入后台」。地址为 http://127.0.0.1:8765/admin.html 。
3. 输入密码，编辑内容后点击「保存到网站」，刷新预览检查。保存只更新本地静态文件，尚未部署到 GitHub Pages。
4. 修改密码仍运行设置程序，需要先验证当前密码。退出登录即刻撤销服务端会话；登录一小时后过期。

当前使用管理员密码，不依赖短信供应商。管理 API 仅绑定本机 127.0.0.1:8766；不会因为知道管理页地址就获得写入权限。浏览器 sessionStorage 只存短期随机会话标识，刷新保持登录。所有保存和上传必须通过服务端验证。

密码校验值使用随机盐与 PBKDF2-HMAC-SHA256（600,000 次），位于 Windows 用户 LocalAppData/RuanStudio/admin-auth.json，不在网站或仓库目录。连续输错五次锁定十五分钟。密码更换立即使旧会话失效。没有默认密码、短信测试码或隐藏免登录入口。尚未设置密码时，写入保持锁定。

## 可维护内容

- 简介与联系：欢迎语、个人简介、学校和专业、课程、联系邮箱。
- 项目：添加/修改/移除/排序，封面、项目类型、年份、简介、问题/过程/结果、链接、简短讲解文稿、PDF、可翻页展示图片。PPTX 请先导出图片或 PDF；此版本不直接解析 PPTX。
- 经历：标题、公司/组织、时间、工作内容、成果、标签和简短讲解；支持添加、移除、排序与撤销上次删除。
- 相册：上传多张照片、修改说明、拖夹子或用方向键排序、删除/撤销。首次载入兼容同源浏览器旧 IndexedDB 相册；保存后写入网站数据，访客页不再用旧本机覆盖已保存数据。旧浏览器记录不清空。

项目与经历的新讲解文稿先以文字展示；不将旧录音用于新文稿。未修改的内容继续使用用户认可的录音和动作。以后需要为新文稿另外生成声音与口型。

## 文件与发布

`content.json` 是管理数据，`content-data.js` 是前台静态数据，媒体在 `assets/uploads/`。保存前自动备份上一版本到项目根目录 `drafts/content-history/`；多窗口基于版本摘要防止静默覆盖。删除条目不会物理销毁原图或附件。

新增照片原先保存在浏览器的数据图在首次保存时迁移成静态文件。新上传文件只有保存内容后才加入前台；放弃编辑可能留下未引用附件，便于后续恢复。导出备份是内容清单，已上传文件仍需随 `assets/uploads/` 一起保留。

发布到现有 GitHub Pages 时，需要提交/部署站点文件；本次没有推送或上线。线上后台 API 尚未部署，公开域名会提示本机管理入口，绝不会向第三方访客的 localhost 发请求。若以后需要手机/异地在线管理，需要部署 HTTPS 管理服务与持久存储，再将后端地址和允许来源设置为实际域名；不要直接把当前本机 API 暴露公网。

## 验证

`tools/check-admin-auth.py` 检查未配置锁定、密码校验、错误次数限制、过期、退出和密码轮换。

`tools/check-admin.cjs` 使用隔离的临时网站文件和临时密码校验值，测试未授权写入/跨域拒绝、登录、项目/经历/照片管理、真实附件写入、刷新保存、冲突拒绝、退出失效、项目前台翻页及手机布局。不会改动用户现有资料或设置用户密码。
