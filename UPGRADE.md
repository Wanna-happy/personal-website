# 当前本地版本 · 2026-09-20

预览：http://127.0.0.1:8765/site/ 。尚未提交或推送线上仓库。

欢迎、认识我与生活、经历、作品、联系共五页，保留深色设计、项目卡片及时间线；没有顶部导航和学位帽图片。

## 自动带领参观

点击欢迎页“带我认识你”或“跟我逛逛”，开始 7 个内容停靠点：学历、产品方向与常用工具、生活、经历、两个项目详情、联系方式。直接从学历内容开讲，不停在 About 标题做泛泛介绍；每个部分只讲重点，照片与经历细节保留主动点击。此次主动点击会开启已有配音，原有音色和口型不变。

项目详情使用非模态展示，保留人物及随人物移动的紧凑讲解浮窗。随配音换句切换“问题 / 过程”，结束后显示“进展”并留出阅读时间，再关闭并介绍下一项目。仅展示已有真实内容，没有虚构 PPT 或原型截图。

底部浮层、导览计数、“你想了解什么？”提示和“减少动画”复选框已移除。讲述和操作共用人物旁原有区域，保留“声音 / 我自己看看 / 再介绍一次”等简洁文字按钮。系统减少动态效果偏好仍然生效。

鼠标点击、滚轮、触屏滑动、滚动按键或离开标签页会暂停自动导览，取消当前语音、移动、滚动和待执行切页。点击内容仍正常响应，项目详情保留。“我自己看看”退出自动流程；其他操作打断后，在同一文字操作栏显示“继续导览”，从当前小节重新开始，并保留用户的静音选择。完成后告别，不自动循环。

`assets/actions/` 新增五段由用户原片提取的情境动作：点头、看向内容、邀请、摊手展示、告别。自动讲解使用已有口型动作，去掉讲解前单独播放手势的等待；点击人物也会轮换回应；不会叠加到已批准的口型讲解上。稳定待机保持不变。

实现：`mascot/auto-guide.js`、`auto-guide.css`。当前验证：`tools/check-concise-guide.cjs` 完整七内容停靠点流程、项目、中断和静音保持；`tools/check-concise-mobile.cjs` 手机与系统减少动画。旧版 `check-auto-guide.cjs`、`check-guide-mobile.cjs` 依赖已移除的浮层，不作为当前验收。动作可用 `tools/prepare-guide-actions.py` 复现。

## 讲解与声音

- 保留用户指定的 `mascot/audio-natural/welcome-preview.wav`，原始文件 SHA256 为 `8878b48a329a33ced83a9222088fe48449ffa6788116f55e4d9083559edd30e3`。
- 其余文稿由本地 Qwen3-TTS 1.7B 以选定声音为参考生成。运行时使用 `audio-continuous/` 的 15 个完整段落，覆盖 32 句字幕；每段加 300 ms 启动缓冲。没有对选定原音做变调或另换音色。
- 音频完整缓存后播放；自然换句只切字幕，不暂停、重载或重新启动音频。手动下一句定位到句子时间点，暂停状态保持，切页/弹窗/跳过可取消旧讲解。
- `assets/lipsync/` 为本机运行 Wav2Lip-SD-GAN 生成的 15 段口型动画，分别对应同名音频，清单保存音频哈希。保留源视频的身体动作，融合模型生成的下半脸，眼睛、头发和服装不由模型重新绘制。
- 音频是主时间轴，口型视频按 1:1 速度播放，暂停和定位跟随音频。模型输出改善匹配，但不承诺每个音素零误差；低分辨率侧脸仍可能有细节偏差。

## 行走与范围

`presenter-roaming.js` 保持单个角色常驻页面，活动范围是整个视口，不再预留底部行走条。点击可用空白处设置目标，优先避开正文和控件；拥挤的小屏可在目标高度的边缘停靠，穿过内容时降低透明度且不拦截点击。正文可正常滚动。

走跑使用真实视频步态，平滑镜头尺度变化并稳定头部位置，避免把每一次抬脚都当作整个身体的上下位移。动作插帧到 60 fps，并在文件内重复步态，减少浏览器短循环重新定位。位移跟随视频时间，缓冲或暂停时不会继续滑动。现有动作仍是二维预制视频，不是三维骨骼；步态接缝和转向存在素材限制。

## 复现及验证

- `tools/package-continuous-narration.py`：完整音频与字幕时间点。
- `tools/local-lipsync.py`：本机 CPU 推理，使用用户原片及批准音色，无生成网站依赖；`tools/publish-lipsync.py` 校验每段音频哈希后接入。
- `tools/smooth-host-gait.py`：步态稳定、选段、补帧和长循环。
- `tools/check-selected-voice.cjs`：批准原音哈希、32 句文本和时间点、可解码音频、缓冲静音与电平。
- `tools/check-host-speech.cjs`：连续播放不重载、跳句、暂停、取消、失败回退与手机布局。
- `tools/check-lipsync-playback.cjs`：15 段模型视频、1:1 播放、时间轴、定位及过期加载取消。
- `tools/check-roaming-sync.cjs`：全屏移动、持续走跑、跨页单一人物、暂停同步、手机及减少动画。

浏览器检查使用本地无头 Edge。模型权重只存于被忽略的 `models/`，不随网站发布。旧版测试包含固定底部和独立句子 MP3 的断言，不代表当前版本。

## 2026-09-20 内容导览细化

- 页面滚动与步行共享视频时间进度，等待步态加载后一起推进；缓冲时不会单独滚动，打断同时取消两者。自动换页跳过默认站位的绕行。
- 自动与手动讲解都使用小光点提示，移除整块内容的描边；保留键盘焦点轮廓。
- 工具卡片由用户确认的墨刀、Axure、Codex、Claude、Workbuddy、Python 构成；图标来源记录于 assets/tools/SOURCES.txt。墨刀仅裁去横版组合标识旁的字标，保留原图形。
- 项目卡片空白与文字区域也可点击打开详情，封面按钮支持键盘；讲解按钮仍独立。后续接入用户自己的 PPT，当前不虚构演示页。
- check-content-guide.cjs：首段学历配音、步行与滚动同步采样、打断取消、继续、六张图标、项目点击/键盘、手机与系统减弱动态。check-roaming-sync.cjs 复测既有音画同步与全屏走动。

## 跑步循环修正

当前跑步资源为 run-clean.mp4，见 mascot/MOTION-ASSETS.md。原 run-smooth.mp4 保留作历史对照，网页已不使用。新跑步素材从原片重新匹配接缝、去掉会产生鞋腿重影的光流补帧，并调低移动速度匹配步频。cache 版本 20260920p。

## 页面空间与相册细化 · 2026-09-20

- 标题统一为“阮涵 | AI创意小屋”，切换章节不覆盖品牌名。
- 取消原 340 px 讲解侧栏，正文占用完整内容宽度。人物控件独立于各章 DOM，讲解时显示短字幕，空闲时收为一行；话题、句子控制和目的地收在“更多”。浮窗在视口内搜索空白位置，避开学历文字、照片、按钮等；按住或悬停操作时保持位置，防止暂停引发点击目标移动。
- 工具区移除大块灰色卡片，只保留真实品牌图标、轻微品牌色底和名称；桌面六列，较窄屏幕三列。
- 生活照片改为错落纸质相册，四张原照保持不变，照片下方显示说明，手机两列；原有三张照片的点击讲解保留。
- 站位自动避让允许跨高度寻找空白，手动点空白移动仍优先保留点击高度。
- 检查：tools/check-compact-layout.cjs 与 tools/check-concise-guide.cjs 均通过；截图 checks/compact-*.png。当前缓存标识 20260920r。
- 待用户提供：完整简介/经历、项目案例内容与 PPT；此轮没有提交、推送或部署。

## 悬停交互与可扩展相册 · 2026-09-20

- 人物浮窗默认隐藏，停留人物 280 ms 后出现；移向控件有 420 ms 缓冲。走动/换页自动隐藏，手机点人物展开，键盘也可操作。
- 首次打开首页，人物头顶出现短欢迎语，8 秒后收起，不随走动闪烁。
- 照片改为绳上的小纸相片，点击看完整大图，支持切换与键盘/触屏操作。
- 空相片入口可选照片并填写说明，添加后自动保留新空位；支持删除、撤销和相册备份。
- 新增照片与删除记录以 IndexedDB 保存，当前仅在同一浏览器/站点地址生效，不代表已发布到 GitHub 或对其他访客可见。维护说明见 ALBUM.md。
- 默认清单 photo-data.js；当前缓存标识 20260920s。三项检查通过：check-hover-album.cjs、check-album-storage.cjs、check-concise-guide.cjs。

## 双主题与相册编辑排序 · 2026-09-20

- 右上角新增深浅色切换；初始保留深色，手动选择写入 localStorage，在样式加载前恢复，跨章节不改变。
- 浅色取自 upstream/personal-website-main/styles.css：米白 #f6f1e8、深棕 #211d19、陶土橙 #c25e3a。小字号辅助文字使用稍深的暖棕以保证阅读。
- 项目、人物控件、照片弹窗、上传表单、滚动条和键盘焦点均适配浅色。
- 照片相框保留原始比例；横图跨两列，挂线自动随每行实际高度调整。文字可编辑、取消并保存；兼容已有本机相册。
- 照片顺序支持电脑拖动“移动”按钮、前移/后移及左右方向键；手机点按钮操作。排序刷新保留，空相片永远位于最后。
- 已通过 check-theme-album.cjs、check-light-detail.cjs、check-album-order.cjs、check-hover-album.cjs；check-concise-guide.cjs 全部七站及项目打断/继续复测通过。
- 当前缓存版本 20260920u。照片修改、排序仍仅保存本浏览器，不自动发布到 GitHub。

## 紧凑相片与本地管理模式 · 2026-09-20

- 根据用户最新截图，照片整体缩小到桌面最大 150 px 宽。宽图高度贴合内容、完整展示，取消跨列加宽和固定高相框产生的留白。
- 移除“移动”按钮，夹子变为真实鼠标/触摸拖动手柄，保留键盘左右排序。
- 默认访客模式无编辑控件，空位显示“尽情期待”。本机 ?album=edit 启用添加、删除、文字编辑、夹子排序；非 loopback 域名不启用此模式。
- 此管理入口不等于账号认证或在线发布。现有浏览器相册照常保留。
- check-compact-owner-album.cjs 通过：访客只读、原图放大、本机管理、鼠标与真实 touch 事件拖动、顺序和文字保存、小宽图完整显示、深浅色及手机边界。缓存版本 20260920w。


## 统一管理后台与密码验证 · 2026-09-20

网站底部新增「网站管理」，统一编辑简介、教育、联系、经历、项目和照片。后台支持项目封面、PDF 和多页图片；经历/项目排序、删除及撤销；保留照片夹子排序。新增内容由前台数据驱动展示，导览项目列表同步更新，修改文稿不再使用旧录音。

管理服务仅监听本机 8766，所有写入检查短期会话。首次密码由用户运行设置程序输入，以带盐 PBKDF2 校验值保存到 LocalAppData，网站和仓库不存密码。未配置时保持锁定，不使用短信测试码。五次输错锁定十五分钟，一小时会话过期；退出或改密码撤销旧会话。

保存更新 content.json、content-data.js 和 assets/uploads，自动备份及版本冲突保护；还未上线。参见 ADMIN.md。旧 ?album=edit 管理模式已退役，现跳转密码后台。检查见 checks/admin-result.json 和 tools/check-admin-auth.py。
